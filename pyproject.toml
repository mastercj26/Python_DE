[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
[project]
name = "data_processor_task"
version = "1.0.0"
description = "A core Python training  project"
readme = "README.md"
requires-python = ">=3.8"
[tool.setuptools]
packages = ["my_processor"]